import { KeyframeResolver as w } from "../../render/utils/KeyframesResolver.js";
import { spring as x } from "../generators/spring/index.js";
import { inertia as _ } from "../generators/inertia.js";
import { keyframes as v } from "../generators/keyframes.js";
import { BaseAnimation as G } from "./BaseAnimation.js";
import { pipe as O } from "../../utils/pipe.js";
import { mix as I } from "../../utils/mix/index.js";
import { calcGeneratorDuration as A } from "../generators/utils/calc-duration.js";
import { millisecondsToSeconds as g, secondsToMilliseconds as B } from "../../utils/time-conversion.js";
import { clamp as C } from "../../utils/clamp.js";
import { invariant as E } from "../../utils/errors.js";
import { frameloopDriver as N } from "./drivers/driver-frameloop.js";
import { getFinalKeyframe as V } from "./waapi/utils/get-final-keyframe.js";
import { isGenerator as j } from "../generators/utils/is-generator.js";
const U = {
  decay: _,
  inertia: _,
  tween: v,
  keyframes: v,
  spring: x
}, W = (P) => P / 100;
class nt extends G {
  constructor(t) {
    super(t), this.holdTime = null, this.cancelTime = null, this.currentTime = 0, this.playbackSpeed = 1, this.pendingPlayState = "running", this.startTime = null, this.state = "idle", this.stop = () => {
      if (this.resolver.cancel(), this.isStopped = !0, this.state === "idle")
        return;
      this.teardown();
      const { onStop: n } = this.options;
      n && n();
    };
    const { name: i, motionValue: r, element: e, keyframes: o } = this.options, u = (e == null ? void 0 : e.KeyframeResolver) || w, a = (n, l) => this.onKeyframesResolved(n, l);
    this.resolver = new u(o, a, i, r, e), this.resolver.scheduleResolve();
  }
  flatten() {
    super.flatten(), this._resolved && Object.assign(this._resolved, this.initPlayback(this._resolved.keyframes));
  }
  initPlayback(t) {
    const { type: i = "keyframes", repeat: r = 0, repeatDelay: e = 0, repeatType: o, velocity: u = 0 } = this.options, a = j(i) ? i : U[i] || v;
    let n, l;
    a !== v && typeof t[0] != "number" && (process.env.NODE_ENV !== "production" && E(t.length === 2, `Only two keyframes currently supported with spring and inertia animations. Trying to animate ${t}`), n = O(W, I(t[0], t[1])), t = [0, 100]);
    const s = a({ ...this.options, keyframes: t });
    o === "mirror" && (l = a({
      ...this.options,
      keyframes: [...t].reverse(),
      velocity: -u
    })), s.calculatedDuration === null && (s.calculatedDuration = A(s));
    const { calculatedDuration: d } = s, p = d + e, f = p * (r + 1) - e;
    return {
      generator: s,
      mirroredGenerator: l,
      mapPercentToKeyframes: n,
      calculatedDuration: d,
      resolvedDuration: p,
      totalDuration: f
    };
  }
  onPostResolved() {
    const { autoplay: t = !0 } = this.options;
    this.play(), this.pendingPlayState === "paused" || !t ? this.pause() : this.state = this.pendingPlayState;
  }
  tick(t, i = !1) {
    const { resolved: r } = this;
    if (!r) {
      const { keyframes: c } = this.options;
      return { done: !0, value: c[c.length - 1] };
    }
    const { finalKeyframe: e, generator: o, mirroredGenerator: u, mapPercentToKeyframes: a, keyframes: n, calculatedDuration: l, totalDuration: s, resolvedDuration: d } = r;
    if (this.startTime === null)
      return o.next(0);
    const { delay: p, repeat: f, repeatType: D, repeatDelay: S, onUpdate: k } = this.options;
    this.speed > 0 ? this.startTime = Math.min(this.startTime, t) : this.speed < 0 && (this.startTime = Math.min(t - s / this.speed, this.startTime)), i ? this.currentTime = t : this.holdTime !== null ? this.currentTime = this.holdTime : this.currentTime = Math.round(t - this.startTime) * this.speed;
    const y = this.currentTime - p * (this.speed >= 0 ? 1 : -1), M = this.speed >= 0 ? y < 0 : y > s;
    this.currentTime = Math.max(y, 0), this.state === "finished" && this.holdTime === null && (this.currentTime = s);
    let b = this.currentTime, K = o;
    if (f) {
      const c = Math.min(this.currentTime, s) / d;
      let T = Math.floor(c), h = c % 1;
      !h && c >= 1 && (h = 1), h === 1 && T--, T = Math.min(T, f + 1), !!(T % 2) && (D === "reverse" ? (h = 1 - h, S && (h -= S / d)) : D === "mirror" && (K = u)), b = C(0, 1, h) * d;
    }
    const m = M ? { done: !1, value: n[0] } : K.next(b);
    a && (m.value = a(m.value));
    let { done: F } = m;
    !M && l !== null && (F = this.speed >= 0 ? this.currentTime >= s : this.currentTime <= 0);
    const R = this.holdTime === null && (this.state === "finished" || this.state === "running" && F);
    return R && e !== void 0 && (m.value = V(n, this.options, e)), k && k(m.value), R && this.finish(), m;
  }
  get duration() {
    const { resolved: t } = this;
    return t ? g(t.calculatedDuration) : 0;
  }
  get time() {
    return g(this.currentTime);
  }
  set time(t) {
    t = B(t), this.currentTime = t, this.holdTime !== null || this.speed === 0 ? this.holdTime = t : this.driver && (this.startTime = this.driver.now() - t / this.speed);
  }
  get speed() {
    return this.playbackSpeed;
  }
  set speed(t) {
    const i = this.playbackSpeed !== t;
    this.playbackSpeed = t, i && (this.time = g(this.currentTime));
  }
  play() {
    if (this.resolver.isScheduled || this.resolver.resume(), !this._resolved) {
      this.pendingPlayState = "running";
      return;
    }
    if (this.isStopped)
      return;
    const { driver: t = N, onPlay: i, startTime: r } = this.options;
    this.driver || (this.driver = t((o) => this.tick(o))), i && i();
    const e = this.driver.now();
    this.holdTime !== null ? this.startTime = e - this.holdTime : this.startTime ? this.state === "finished" && (this.startTime = e) : this.startTime = r ?? this.calcStartTime(), this.state === "finished" && this.updateFinishedPromise(), this.cancelTime = this.startTime, this.holdTime = null, this.state = "running", this.driver.start();
  }
  pause() {
    var t;
    if (!this._resolved) {
      this.pendingPlayState = "paused";
      return;
    }
    this.state = "paused", this.holdTime = (t = this.currentTime) !== null && t !== void 0 ? t : 0;
  }
  complete() {
    this.state !== "running" && this.play(), this.pendingPlayState = this.state = "finished", this.holdTime = null;
  }
  finish() {
    this.teardown(), this.state = "finished";
    const { onComplete: t } = this.options;
    t && t();
  }
  cancel() {
    this.cancelTime !== null && this.tick(this.cancelTime), this.teardown(), this.updateFinishedPromise();
  }
  teardown() {
    this.state = "idle", this.stopDriver(), this.resolveFinishedPromise(), this.updateFinishedPromise(), this.startTime = this.cancelTime = null, this.resolver.cancel();
  }
  stopDriver() {
    this.driver && (this.driver.stop(), this.driver = void 0);
  }
  sample(t) {
    return this.startTime = 0, this.tick(t, !0);
  }
}
export {
  nt as MainThreadAnimation
};
